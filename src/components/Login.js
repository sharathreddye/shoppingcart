import React, { Component } from 'react';


class Login extends Component {
  render() {
    return (

        <div>Check out screen after login only user will access...</div>
    );
 }
}
export default Login;
